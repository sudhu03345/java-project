import java.Util*;
class vector_demo
{
	public static void main(String args[])
	{
		vector list=args.length;
		for int(i=0,i<len;i++)
		{
			list add.length(args[i])
		}
		list insert element AT("os",1);
		int s s=list.size();
		String array=new String[s];
		list.copyInt 0 (array);
		System.out.println("list of elements:");
		for(int i=0;i<len;i++)
		{
			System.out.println(list of element[i]);
		}
	}
}