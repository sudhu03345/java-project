class array_demo
{
	public static void main (String args[])
	{
		int[]=new in length();
		int legnth=args.length():
		int i;
		for(i=0;1<len;i++)
		{
			x[i]=integer.parseInt(args[i]);
		}
		for(i=10;i<len;i++)
		{
			system.out.println(x[i]);
		}
		sum=sum+i
		avg=sum/i
	}(exception e){}
}